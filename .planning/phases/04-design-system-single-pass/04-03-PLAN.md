---
phase: 04-design-system-single-pass
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - frontend/src/RegistryEditorModal.tsx
  - frontend/src/LoginScreen.tsx
  - frontend/src/ui/Modal.tsx
  - frontend/src/ui/Topbar.tsx
autonomous: true
requirements: [DESIGN-02]

estimate:
  tokens: 26000
  raw_tokens: 20000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The last two screens still carrying raw Tailwind palette classes, the registry editor modal and the login screen, express their error banners through the red token in the same shape every other error banner in the app already uses (DESIGN-02, D-02c)"
    - "After this plan and plan 04-02 land, the only raw palette class left anywhere in frontend/src is the documented literal device-colour swatch in ColorChip"
    - "Every icon-only control in the app shell announces itself to a screen reader, closing the accessibility flag the UI-SPEC checker raised against Topbar and the Modal close button"
    - "Nothing visual changes in Modal or Topbar: the accessibility work adds attributes only, no class, no layout, no icon"
    - "No handler, conditional, validation rule or API call changed in any of the four files (D-06)"
  artifacts:
    - frontend/src/RegistryEditorModal.tsx
    - frontend/src/LoginScreen.tsx
    - frontend/src/ui/Modal.tsx
    - frontend/src/ui/Topbar.tsx
  key_links:
    - "This plan and plan 04-02 must together drive the app-wide raw-palette count to exactly 1; plan 04-04's build guard cannot be switched on until both have landed"
    - "The error-banner shape in CheckinWizard.tsx line 614 is the reference both banners converge on, so all three read identically"
---

<objective>
Close the last two raw-palette screens and the accessibility flag the UI-SPEC checker raised, so
that after this wave the only raw Tailwind colour class anywhere in the frontend is the one
documented as legitimate.

Purpose: DESIGN-02 and D-02c scope the consistency pass across every screen, not only the ones
UI-SPEC sampled. A direct grep of the live tree during planning found two small residual instances
outside UI-SPEC's inventory, in RegistryEditorModal.tsx and LoginScreen.tsx. Leaving them would
make the DESIGN-02 claim false and would force plan 04-04's build guard to carry two arbitrary
exceptions instead of the one genuinely documented one. The UI-SPEC checker's Dimension 2 flag —
icon-only app-shell chrome with no declared accessible name — is grouped here because it is the
other small, zero-visual-risk item the approved design contract explicitly recommends doing during
this pass.

Output: four files, two of them with zero raw palette classes and two with complete accessible
names on icon-only controls.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-design-system-single-pass/04-CONTEXT.md
@.planning/phases/04-design-system-single-pass/04-UI-SPEC.md
@GEMINI.md
</context>

## Artifacts this phase produces

No new files or symbols. This plan modifies only existing markup:

| Artifact | Kind | Where |
|----------|------|-------|
| error-banner class string | modified | frontend/src/RegistryEditorModal.tsx line 123 |
| error-banner class string | modified | frontend/src/LoginScreen.tsx line 95 |
| `aria-label` on the close control | added attribute | frontend/src/ui/Modal.tsx line 53 |
| `aria-label` on icon-only chrome controls | added attributes | frontend/src/ui/Topbar.tsx lines 58, 73, 82, 113 |

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Convert the last two raw-palette error banners to the red token (D-02c)</name>

  <read_first>
    - frontend/src/RegistryEditorModal.tsx — read lines 115-130. The file is 133 lines; line 123 holds the only drift instance.
    - frontend/src/LoginScreen.tsx — read lines 88-102. The file is 159 lines; line 95 holds the only drift instance.
    - frontend/src/CheckinWizard.tsx line 614 — the canonical error-banner shape in this codebase, a red-token border, a red-token background at low alpha and red-token text. Both banners converge on this.
    - .planning/phases/04-design-system-single-pass/04-UI-SPEC.md — the Color table's Destructive row, which names the exact token and the established error-banner composition.
    - .planning/phases/04-design-system-single-pass/04-CONTEXT.md — D-02 item c and D-06.
  </read_first>

  <files>frontend/src/RegistryEditorModal.tsx, frontend/src/LoginScreen.tsx</files>

  <action>
Neither of these two lines appears in UI-SPEC's or PATTERNS' drift inventory, because UI-SPEC's
research sampled PosScreen, App, Kanban and CheckinWizard only. A direct grep of the live tree
during planning found them, and they are the identical category of fix as the locked D-02b
instances: a semantic meaning expressed through a raw Tailwind palette family where a token already
exists. Leaving them out would make the DESIGN-02 claim that every screen reflects the design
system untrue, so they are folded into the pass here.

RegistryEditorModal.tsx line 123: the inline error paragraph composes its border, background and
text from the raw red palette family. Rewrite the class string on the red token, matching the
border and background alphas the reference banner at CheckinWizard.tsx line 614 uses, and taking
the solid red token for the text.

LoginScreen.tsx line 95: this banner already uses the red token for its border and background and
only its text colour is raw. Change only the text colour to the solid red token; leave the border,
background, radius, padding, layout and gap classes exactly as they are.

Change nothing else in either file. No conditional, no handler, no error-message string, no state.
  </action>

  <verify>
    <automated>wsl -d Ubuntu -e bash -c "cd /home/usuario/frappe-bench/apps/tecponto_app/frontend && grep -coE '\b(bg|text|border|ring|from|to|via|divide|outline|decoration|shadow|accent|caret|fill|stroke)-(red|rose|emerald|green|amber|slate|zinc|gray|neutral|stone|yellow|blue|purple|pink|indigo|violet|teal|cyan|sky|lime|fuchsia)-[0-9]{2,3}\b' src/RegistryEditorModal.tsx src/LoginScreen.tsx && npm run typecheck"</automated>
    <fails_when>The grep prints anything other than the two lines "src/RegistryEditorModal.tsx:0" and "src/LoginScreen.tsx:0" (baseline is 1 and 1), or typecheck exits non-zero or prints a line containing "error TS".</fails_when>
  </verify>

  <acceptance_criteria>
    - The grep in the verify command prints 0 for both files.
    - `wsl -d Ubuntu -e bash -c "cd /home/usuario/frappe-bench/apps/tecponto_app/frontend && grep -c 'tec-red' src/RegistryEditorModal.tsx src/LoginScreen.tsx"` prints at least 1 for each file.
    - `wsl -d Ubuntu -e bash -c "cd /home/usuario/frappe-bench/apps/tecponto_app/frontend && git diff -U0 -- src/RegistryEditorModal.tsx src/LoginScreen.tsx | grep -c '^[-+][^-+]'"` prints 4 — one removed and one added line per file, confirming a pure class-string substitution.
    - `wsl -d Ubuntu -e bash -c "cd /home/usuario/frappe-bench/apps/tecponto_app/frontend && grep -c '{error' src/RegistryEditorModal.tsx src/LoginScreen.tsx"` prints the same counts as before the edit — the error conditionals survived.
    - Typecheck exits 0.
  </acceptance_criteria>

  <done>Both files carry zero raw palette classes, both error banners read as the same red-token shape the rest of the app uses, and the diff is two substituted lines with no logic change.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Give every icon-only app-shell control an accessible name</name>

  <read_first>
    - frontend/src/ui/Modal.tsx — the whole file, roughly 60 lines. Line 46 already sets an aria-label on the dialog container; line 53 is the icon-only close control that has none.
    - frontend/src/ui/Topbar.tsx — the whole file, 120 lines. Lines 58, 73, 82 and 113 carry a title attribute but no aria-label; line 93 already carries both and is the pattern to copy.
    - .planning/phases/04-design-system-single-pass/04-UI-SPEC.md — the Checker Sign-Off section, Dimension 2, which raises this as a flag and recommends confirming or adding during this pass.
  </read_first>

  <files>frontend/src/ui/Modal.tsx, frontend/src/ui/Topbar.tsx</files>

  <action>
The UI-SPEC checker flagged that icon-only chrome has no declared accessible-name fallback. Reading
the live source narrows the real gap: Topbar's theme toggle at line 93 already carries both an
aria-label and a title, so that is the in-repo pattern; the other four Topbar controls at lines 58,
73, 82 and 113 carry only a title, and Modal's close control at line 53 carries neither, since the
aria-label at line 46 names the dialog itself rather than its close button.

Add an explicit aria-label to Modal's close control and to the four Topbar controls that lack one,
matching each control's existing title text so the spoken name and the tooltip agree. Copy the shape
used at Topbar line 93 exactly: both attributes present, same string. For Modal, the label should
name the close action rather than repeat the dialog title, so it does not collide with the
container's aria-label at line 46.

Add attributes only. Do not touch a className, an icon, a size, a layout wrapper, a handler or a
variant in either file — this task must produce zero visual change, which is why it is grouped with
the class-string substitution in Task 1 rather than with any restyling work.
  </action>

  <verify>
    <automated>wsl -d Ubuntu -e bash -c "cd /home/usuario/frappe-bench/apps/tecponto_app/frontend && grep -c 'aria-label' src/ui/Modal.tsx src/ui/Topbar.tsx && npm run build"</automated>
    <fails_when>The counts are not at least 2 for Modal.tsx and at least 5 for Topbar.tsx (baselines are 1 and 1), or the build exits non-zero, or the build output contains "error TS", "Token ausente", "Termo sensível no front", "Etapa obrigatória ausente", "Ação existente da OS deixou de estar conectada", or the wizard-alignment message added by plan 04-01.</fails_when>
  </verify>

  <acceptance_criteria>
    - `wsl -d Ubuntu -e bash -c "cd /home/usuario/frappe-bench/apps/tecponto_app/frontend && grep -c 'aria-label' src/ui/Modal.tsx"` prints at least 2 and `... src/ui/Topbar.tsx` prints at least 5.
    - `wsl -d Ubuntu -e bash -c "cd /home/usuario/frappe-bench/apps/tecponto_app/frontend && git diff -U0 -- src/ui/Modal.tsx src/ui/Topbar.tsx | grep -cE '^[-+].*className'"` prints 0 — no class string was touched, so the change is visually inert.
    - `wsl -d Ubuntu -e bash -c "cd /home/usuario/frappe-bench/apps/tecponto_app/frontend && grep -c 'onClick' src/ui/Modal.tsx src/ui/Topbar.tsx"` prints the same counts as before the edit.
    - `wsl -d Ubuntu -e bash -c "npm --prefix /home/usuario/frappe-bench/apps/tecponto_app/frontend run build"` exits 0.
  </acceptance_criteria>

  <done>Every icon-only control in Modal and Topbar has an aria-label matching its tooltip, no className anywhere in the diff, and the build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated visitor -> LoginScreen | The only pre-authentication surface this phase touches. Only the error banner's text colour changes; no field, handler, credential path or error string is edited. |
| browser -> Frappe API | Unchanged. No endpoint, serializer, role gate or payload field is touched. |
| Diretor-only registry data -> RegistryEditorModal | verify-foundation.mjs grants this file a narrow director-only exception for one valuation term (line 70). Edits here must not widen that surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-11 | Information Disclosure | RegistryEditorModal.tsx, which holds a director-only cost-term exception in the build guard | medium | mitigate | The edit is one class string on the error paragraph at line 123; no field, serializer or role conditional is read or written. Task 2's verify runs a full `npm run build`, re-running the forbidden-term loop over every src file including this one. |
| T-04-12 | Information Disclosure | LoginScreen.tsx error banner | low | mitigate | Only the text colour class changes. The error message strings themselves are untouched, so the banner cannot start disclosing more than it did, and the acceptance criteria assert the error conditional count is unchanged. |
| T-04-13 | Spoofing | Modal and Topbar aria-labels | low | accept | Labels are copied from each control's existing title text, so the announced name matches the visible tooltip. An aria-label carries no authorization meaning and cannot alter what a control does. |
| T-04-14 | Tampering | The 6 OS lifecycle locks and role gates | low | accept | None of these four files contains a lifecycle transition, a role gate or a validation rule; they are a modal shell, an app-shell header, a login form and a registry editor's error line. Plan 04-05 runs the full backend suite as the phase-level regression proof. |
| T-04-15 | Tampering | Dependency supply chain | low | accept | No package-manager install occurs. frontend/package.json is not modified and no new import is introduced, so no package-legitimacy audit applies. |
</threat_model>

<verification>
1. `wsl -d Ubuntu -e bash -c "npm --prefix /home/usuario/frappe-bench/apps/tecponto_app/frontend run build"` exits 0.
2. The raw-palette scan prints 0 for RegistryEditorModal.tsx and LoginScreen.tsx, down from 1 each.
3. The Modal and Topbar diff contains no className line at all.
4. Once this plan and plan 04-02 have both landed, the app-wide scan `grep -rhoE <raw-palette-family> src --include='*.tsx' --include='*.ts' | wc -l` prints 1, down from the pre-phase baseline of 65 — the single survivor being the documented literal device-colour swatch in ColorChip.
</verification>

<success_criteria>
- No raw Tailwind palette class remains in either screen file.
- Both error banners read as the same red-token shape as the app's other error banners.
- Every icon-only Modal and Topbar control announces a name matching its tooltip, with zero visual change.
- `npm run build` passes.
</success_criteria>

<output>
Create `.planning/phases/04-design-system-single-pass/04-03-SUMMARY.md` when done.
</output>
