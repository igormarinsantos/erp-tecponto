---
phase: 04-design-system-single-pass
plan: 05
type: execute
wave: 4
depends_on: ["04-01", "04-02", "04-03", "04-04"]
files_modified: []
autonomous: true
requirements: [DESIGN-01, DESIGN-02]

estimate:
  tokens: 38000
  raw_tokens: 29000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The full frontend build passes with every guard active: typecheck, Vite, required tokens, the cost-term leak scan, the OS stage and action assertions, the wizard step-alignment guard and the raw-palette guard"
    - "The full backend behavioural suite still passes after the visual pass, proving no lifecycle lock, role gate, cost guard or credential sentinel was disturbed (ROADMAP Phase 4 success criterion 3)"
    - "A live click-through of each journey across all four roles confirms nothing functional broke and every screen reflects the documented design system (ROADMAP Phase 4 success criteria 2 and 3)"
    - "The check-in wizard's 7 steps are walked on a real narrow viewport, settling the stepper-overflow backstop UI-SPEC left open"
    - "Both themes are exercised, settling the light-theme inversion of the subtle token that plan 04-01 flagged on the Prata swatch"
    - "Any divergence found during the click-through that is functional rather than visual is written up as a deferred item rather than fixed (D-06)"
    - "Phase 4 touched zero Python files, so the backend run is a pure regression check with no new behaviour to prove"
  artifacts: []
  key_links:
    - "The frontend build and the backend suite must both be green against the same working tree; running them against different states would prove nothing"
    - "The click-through findings and the deferred-items list: a visual complaint gets fixed, a functional divergence gets deferred per D-06, and conflating the two is how a visual-only phase turns into a redesign"
---

<objective>
Compose the phase-level verification ROADMAP Phase 4 success criterion 3 requires: the full
behavioural test suite still passes after the visual pass, and a live click-through of each journey
confirms nothing functional broke.

Purpose: plans 04-01 through 04-04 each verified their own slice. This plan proves the whole phase
at once, against one working tree — the frontend build with all guards, the backend suite as a
regression check, and human eyes on the four role journeys. It is also where the three open
judgement calls this phase deliberately deferred get settled: the stepper at 7 columns on a narrow
viewport, the light-theme rendering of the Prata swatch, and whether the check-in-level settings
card left pinned to the Defeito sub-step keeps that step feeling dense.

Output: a verification record, a deferred-items list, and the decision on those three open calls.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-design-system-single-pass/04-CONTEXT.md
@.planning/phases/04-design-system-single-pass/04-UI-SPEC.md
@GEMINI.md
</context>

## Artifacts this phase produces

This plan creates no source artifact. It produces the phase's verification record in
`.planning/phases/04-design-system-single-pass/04-05-SUMMARY.md`, which must contain:

| Record | Content |
|--------|---------|
| Build result | Exit status and output of the full frontend build with all six guards active |
| Backend regression result | Exit status of the foundation suite, plus whether local Docker flaked and CI was deferred to |
| Click-through log | One line per role journey walked, naming what was observed |
| Three settled calls | Narrow-viewport stepper, light-theme Prata swatch, Defeito sub-step density |
| Deferred items | Every functional divergence found during the OS-vs-PDV comparison, per D-06 |

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Full frontend build with every guard active</name>

  <read_first>
    - The SUMMARY files from plans 04-01, 04-02, 04-03 and 04-04, for what each landed and what each flagged.
    - frontend/scripts/verify-foundation.mjs as edited by plans 04-01 and 04-04, so the expected guard output is known before running.
  </read_first>

  <files>none — verification only</files>

  <action>
Confirm the working tree contains all four prior plans' changes before running anything; a green
build against a partial tree proves nothing. Then run the full frontend build, which chains
typecheck, the Vite production build and verify-foundation.mjs with all six of its checks: required
build artefacts, required tokens, the forbidden cost-term scan, the OS stage-label and preserved-
action assertions, the wizard step-alignment guard from plan 04-01 and the raw-palette guard from
plan 04-04.

Also run the app-wide raw-palette scan directly and record the number. The pre-phase baseline
measured during planning was 65 instances across 8 files; the expected post-phase figure is exactly
1, the documented literal device-colour swatch in ColorChip. Any other number means a screen was
missed or the guard's exception is mis-scoped, and it must be reconciled before this plan closes.

If the build fails, do not patch around it: identify which plan's change caused it, record it, and
treat it as a gap for that plan rather than absorbing the fix here.
  </action>

  <verify>
    <automated>wsl -d Ubuntu -e bash -c "npm --prefix /home/usuario/frappe-bench/apps/tecponto_app/frontend run build"</automated>
    <fails_when>Non-zero exit, or output containing "error TS", "Build ausente", "Token ausente", "Termo sensível no front", "Etapa obrigatória ausente", "Ação existente da OS deixou de estar conectada", the wizard step-alignment message from plan 04-01, or the raw-palette message from plan 04-04. A clean run ends with the existing foundation-verified line.</fails_when>
  </verify>

  <acceptance_criteria>
    - The build exits 0 and its final line is the existing foundation-verified confirmation.
    - `wsl -d Ubuntu -e bash -c "cd /home/usuario/frappe-bench/apps/tecponto_app/frontend && grep -rhoE '\b(bg|text|border|ring|from|to|via|divide|outline|decoration|shadow|accent|caret|fill|stroke)-(red|rose|emerald|green|amber|slate|zinc|gray|neutral|stone|yellow|blue|purple|pink|indigo|violet|teal|cyan|sky|lime|fuchsia)-[0-9]{2,3}\b' src --include='*.tsx' --include='*.ts' | wc -l"` prints exactly 1, down from the pre-phase baseline of 65.
    - `wsl -d Ubuntu -e bash -c "cd /home/usuario/frappe-bench/apps/tecponto_app && git status --short -- '*.py'"` prints nothing — Phase 4 touched zero Python files, which is what makes Task 2 a pure regression run.
    - The SUMMARY records the build's exit status and the measured raw-palette count.
  </acceptance_criteria>

  <done>The full frontend build passes with all six guards active, and exactly one documented raw palette class remains in the entire source tree.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Backend behavioural suite as a post-visual-pass regression check</name>

  <precondition>The local Docker container `tecponto-local-server` must be reachable; if `./scripts/dev-local-server.sh restart` cannot bring it up after one retry, classify it as environment noise per the project's known Docker/WSL2 instability, record that CI is the integration authority per GEMINI.md section 4, and do not block the phase on it.</precondition>

  <read_first>
    - GEMINI.md section 4, for the project's validation commands and the standing rule that the CI pipeline is the final integration truth.
    - .planning/STATE.md, Blockers and Concerns — the recorded dev-local-server restart failure and the chronic Docker/WSL2 instability, so a container flake is recognised as environment noise rather than misread as a Phase 4 regression.
    - The SUMMARY from plan 04-01, specifically its note about the device-access credential block moving intact during the sub-step split, which the sentinel checks in this suite are the real proof of.
  </read_first>

  <files>none — verification only</files>

  <action>
Run the foundation suite exactly once, against the same working tree Task 1 built. Phase 4 changed
no Python and added no backend behaviour, so there is nothing new to test here — this run exists
solely to prove that the visual pass disturbed nothing: the 6 OS lifecycle locks, the role gates,
the cost and margin guard, and the device-credential sentinel checks that GEMINI.md section 1.2
requires to keep passing continuously.

Use the project's established shape: restart the local server, then execute the foundation checks
inside the container against the local CI site.

If the container goes down or the WSL VM reboots mid-run, that is the known environment instability
recorded in STATE.md, not a Phase 4 regression. Retry once. If it flakes again, record the attempt
and defer to the CI pipeline as the integration authority per GEMINI.md section 4 — and say so
explicitly in the SUMMARY rather than silently marking the criterion satisfied.
  </action>

  <verify>
    <automated>wsl -d Ubuntu -e bash -c "cd /home/usuario/frappe-bench/apps/tecponto_app && ./scripts/dev-local-server.sh restart && docker exec tecponto-local-server bash -lc 'cd /home/frappe/frappe-bench && bench --site local-ci.local execute tecponto_app.tecponto.frontend.test_frontend_api.run_foundation_checks'"</automated>
    <fails_when>Non-zero exit, or output containing "Traceback", "AssertionError", "FAILED" or "ValidationError". Distinguish two failure classes: a container or connection failure ("Cannot connect to the Docker daemon", "No such container", "Connection refused", "Error response from daemon") is environment noise — retry once, then defer to CI. Anything naming a lifecycle lock, a role gate, a cost or margin field, or a credential sentinel is a real Phase 4 regression and blocks the phase.</fails_when>
  </verify>

  <acceptance_criteria>
    - The suite exits 0, or the SUMMARY explicitly records the container failure, the single retry, and the deferral to CI as the integration authority.
    - No assertion naming a cost, margin or profit field appears in the output.
    - No assertion naming the device access credential or a sentinel value appears in the output.
    - The SUMMARY states that Phase 4 modified zero Python files, so this run is a regression check with no new backend behaviour under test.
  </acceptance_criteria>

  <done>The foundation suite passes against the post-pass tree, or its failure is proven to be environment noise and CI is named as the deferred authority.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Live click-through of every role journey, and settle the three open calls</name>

  <read_first>
    - .planning/ROADMAP.md, Phase 4 success criteria 2 and 3, which require every screen across all four roles to reflect the design system and a live click-through of each journey.
    - .planning/phases/04-design-system-single-pass/04-UI-SPEC.md — the UI Considerations table, specifically the overflow row marked as a backstop requiring exactly this click-through.
    - .planning/phases/04-design-system-single-pass/04-CONTEXT.md — D-05 and D-06, so a functional divergence is deferred rather than fixed.
    - The SUMMARY files from plans 04-01 and 04-02, for the two flagged visual consequences: the light-theme inversion of the subtle token on the Prata swatch, and the app-wide re-toning of the danger button's text.
    - frontend/DESIGN_SYSTEM.md as written by plan 04-04, which is the reference the click-through judges each screen against.
  </read_first>

  <files>none — verification only</files>

  <action>
Start the dev server and walk the real journeys in a browser. This is the criterion GEMINI.md
section 2.2 insists on — nothing counts as done until the real flow has been exercised end to end,
and a button that merely renders is not a button that works.

Present the developer with a concrete walk list rather than an open-ended "check the app": the
Atendente check-in from Cliente through Revisao across all 7 steps; a PDV sale including the clear-
sale destructive confirmation; the Gestor and Tecnico views of an OS including the budget-decision
approve and reject surfaces and the Kanban board with at least one overdue card; the quotes CRM
screen's status pills and its approve and reject actions; and the Diretor view, which is the only
role that may see cost and margin data at all.

Three specific calls must be settled and written down, not left implicit:
- Walk the 7-step wizard at a narrow viewport width. UI-SPEC marked the stepper overflow as a
  backstop precisely because going from five to seven columns narrows every label cell, and only a
  real narrow viewport can say whether truncation is acceptable or the labels became unreadable.
- Toggle the theme and look at the Prata device-colour swatch in both. Plan 04-01 applied the locked
  substitution and flagged that the underlying token inverts between themes, so the swatch reads as
  light silver in one and dark graphite in the other. Decide whether that is acceptable.
- On the Defeito sub-step, judge whether the check-in-level settings card that stayed pinned to the
  top of it leaves that step as dense as the old combined Dados step was. Reducing that density is
  the entire reason D-03 exists, so if it still feels dense, that is a real finding.

Also compare the OS budget-decision card against the PDV surfaces side by side, per D-05. If any
divergence found is functional — different validation, different data shown for the same concept —
record it as a deferred item under D-06 and do not fix it here.

Report findings by category: visual problems inside this phase's scope, visual debt already logged
in DESIGN_SYSTEM.md's known-debt section, and functional divergences to defer. Do not fix a
functional divergence in this plan under any circumstance.
  </action>

  <verify>
    <human-check>
Start the dev server with `wsl -d Ubuntu -e bash -c "npm --prefix /home/usuario/frappe-bench/apps/tecponto_app/frontend run dev"` and confirm each of the following in the browser:

1. Check-in (Atendente): the stepper shows 7 steps labelled Cliente, Aparelho, Defeito, Estado fisico, Acessorios, Fotos, Revisao. Avancar is blocked on Defeito until the client report is typed, blocked on Estado fisico until one physical-state box is ticked, and never blocked on Acessorios. The banner counter reads "de 7". Completing the wizard creates an OS whose detail view shows the same reported defect, physical state, accessories and notes that were entered.
2. Narrow viewport: repeat the stepper view at a phone-width window. State whether the 7 labels are readable or unacceptably truncated.
3. Theme toggle: on the Aparelho or device-colour step, look at the Prata swatch in both dark and light themes. State whether the light-theme rendering is acceptable.
4. Defeito density: state whether the Defeito sub-step, with the contact-and-access settings card still at its top, feels lighter than the old combined Dados step or just as dense.
5. PDV (Atendente/Gestor): run a sale, open the clear-sale confirmation, confirm the destructive button still reads and behaves as before.
6. OS budget decision (Gestor): the approve, reject, expired and pending pills render in their expected colours; the approve and reject buttons are visually distinct from each other and both work end to end.
7. Kanban (Gestor/Tecnico): an overdue card still shows its red border and its "Atrasada" label.
8. Quotes CRM: status pills and the approve and reject actions render and work.
9. Diretor: the financial view still shows what only this role may see, and no other role's screen shows any cost, margin or profit figure.
10. OS versus PDV: state whether equivalent patterns now look consistent, and list any divergence that is functional rather than visual.

Answer yes or no on each, and describe anything that looks wrong.
    </human-check>
  </verify>

  <acceptance_criteria>
    - All ten click-through items are answered, each with an explicit yes or no and an observation.
    - The three open calls — narrow-viewport stepper, light-theme Prata swatch, Defeito sub-step density — each have a recorded decision in the SUMMARY, not a blank.
    - Every functional divergence found is listed under a deferred-items heading in the SUMMARY with the reason it was not fixed, citing D-06.
    - The SUMMARY states plainly whether ROADMAP Phase 4 success criteria 2 and 3 are met, and if not, exactly which item failed.
    - No source file was modified by this plan: `wsl -d Ubuntu -e bash -c "cd /home/usuario/frappe-bench/apps/tecponto_app && git status --short -- frontend/src"` prints nothing beyond what plans 04-01 through 04-04 already committed.
  </acceptance_criteria>

  <done>All four role journeys were walked live, the three open calls are settled and recorded, functional divergences are deferred rather than fixed, and the SUMMARY states whether the phase's success criteria are met.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Atendente, Gestor and Tecnico views -> cost, margin and profit data | GEMINI.md section 1.1. This plan is where the guard is verified end to end rather than only by static scan: click-through item 9 checks it at the surface, and Task 2's suite checks it in the payload. |
| Any view -> device access credential | GEMINI.md section 1.2. The credential block physically moved during plan 04-01's sub-step split, so the sentinel checks in Task 2's suite are this phase's real proof that it did not start leaking. |
| browser -> Frappe API | Unchanged by the phase. This plan modifies nothing; it only observes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-21 | Information Disclosure | Device access credential after the wizard sub-step split | high | mitigate | Task 2 runs the foundation suite, whose continuous sentinel checks prove the credential does not appear in OS lists, public detail, budgets, thermal or A4 prints, tracking links, WhatsApp messages or unauthorised responses. An assertion naming a credential or sentinel is explicitly classified as a blocking regression, not environment noise. |
| T-04-22 | Information Disclosure | Cost, margin and profit fields reaching a non-Diretor role | high | mitigate | Two independent checks: Task 1's build re-runs the forbidden-term scan over every frontend source file, and Task 2's backend suite checks the payload itself. Click-through item 9 adds a human surface check across all four roles. |
| T-04-23 | Tampering / Elevation of Privilege | The 6 OS lifecycle locks and role gates | high | mitigate | Task 1 asserts zero Python files changed in the phase, which makes a backend regression structurally implausible; Task 2 proves it empirically; click-through items 5, 6 and 7 exercise the guarded transitions by hand. |
| T-04-24 | Denial of Service (usability) | The 7-step wizard at narrow viewport, or a step made unreachable | medium | mitigate | Click-through items 1 and 2 walk all 7 steps including at phone width, and the build-time step-alignment guard from plan 04-01 would already have failed on a desync. |
| T-04-25 | Repudiation | A visual-only phase quietly absorbing a functional fix | medium | mitigate | The action and the acceptance criteria both forbid fixing a functional divergence in this plan and require it to be written to a deferred-items list citing D-06. |
| T-04-26 | Tampering | Dependency supply chain | low | accept | No package-manager install occurs anywhere in Phase 4. No plan lists frontend/package.json in files_modified and no new package was introduced, so the package-legitimacy gate does not apply. |
</threat_model>

<verification>
1. The full frontend build exits 0 with all six guards active.
2. The app-wide raw-palette count is exactly 1, down from 65.
3. `git status --short -- '*.py'` prints nothing for the phase.
4. The backend foundation suite exits 0, or its failure is proven to be environment noise with CI named as the deferred authority.
5. All ten click-through items answered, the three open calls settled, and functional divergences deferred under D-06.
</verification>

<success_criteria>
- ROADMAP Phase 4 success criterion 3 is satisfied: the behavioural suite passes after the visual pass and a live click-through of each journey confirms nothing functional broke.
- ROADMAP Phase 4 success criterion 2 is satisfied: every screen across all four roles reflects the documented design system, checked against frontend/DESIGN_SYSTEM.md.
- The three deliberately deferred judgement calls are settled and written down.
- Every functional divergence is deferred, not fixed.
</success_criteria>

<output>
Create `.planning/phases/04-design-system-single-pass/04-05-SUMMARY.md` when done.
</output>
