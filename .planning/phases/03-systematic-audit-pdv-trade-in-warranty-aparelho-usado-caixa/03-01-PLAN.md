---
phase: 03-systematic-audit-pdv-trade-in-warranty-aparelho-usado-caixa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tecponto_app/tecponto/frontend/test_frontend_api.py
autonomous: true
requirements: [AUDIT-01, AUDIT-02]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Running run_foundation_checks exercises the PDV sale, repair-warranty-mode, PDV barcode-label and PDV retail-barcode-catalog suites — they are no longer dead code (D-01)"
    - "A second cash-session opening at the same cash_point/business_date is proven to be rejected by the server, inside CI (D-02)"
    - "Closing a cash session with an undocumented divergence remains proven-blocked by run_cash_closing_checks"
  artifacts:
    - "tecponto_app/tecponto/frontend/test_frontend_api.py — run_foundation_checks calls and returns all 4 previously-orphaned checks"
    - "tecponto_app/tecponto/frontend/test_frontend_api.py — run_cash_session_checks carries a second_open_blocked assertion"
  key_links:
    - "run_foundation_checks return dict -> the 4 new keys (pos_sale / warranty_mode / pos_barcode_label / pos_retail_barcode_catalog) — if a key is missing the suite silently loses the coverage again"
    - "run_cash_session_checks -> cash.py::open_cash_session session_key collision branch (cash.py:56-64) — the assertion must hit the session_key branch, not the opening_idempotency_key replay branch"
---

<objective>
Close the single largest CI blind spot in this phase at near-zero risk: wire the four already-passing but never-called PDV/warranty-mode test functions into the live suite (D-01), then prove the already-implemented concurrent-cash-session hard block actually fires (D-02).

Purpose: AUDIT-01 and AUDIT-02 are almost entirely already-correct production behavior with missing CI coverage. Wiring first means every later plan in this phase runs against a suite that already watches PDV and caixa — so a regression introduced by AUDIT-03/AUDIT-04/AUDIT-05 work is caught immediately instead of at the end.
Output: `run_foundation_checks` exercises 4 more domains; `run_cash_session_checks` proves the second-opening block.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-systematic-audit-pdv-trade-in-warranty-aparelho-usado-caixa/03-CONTEXT.md
@.planning/phases/03-systematic-audit-pdv-trade-in-warranty-aparelho-usado-caixa/03-PATTERNS.md
@GEMINI.md

Environment notes for the executor:
- Source of truth is the WSL repo at `/home/usuario/frappe-bench/apps/tecponto_app` (branch `version-16`). Every shell command below is written to be run as-is from the Windows side via `wsl -d Ubuntu -e bash -c "..."`.
- After ANY Python change, `./scripts/dev-local-server.sh restart` MUST run before re-testing — the local bench runs with `--noreload` and will otherwise serve the old module from memory (GEMINI.md section 2.5).
- Known environment instability, NOT a code defect: the Docker/WSL2 container `tecponto-local-server` periodically dies on its own (`OOMKilled=false`, `ExitCode=255`, no application error — a Docker Desktop host power-management issue confirmed during discussion). If a verify command needs two or three attempts to get a clean run, that is environmental. Re-run `./scripts/dev-local-server.sh up` and retry; do not "fix" the test.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Wire the 4 orphaned PDV/warranty checks into the live suite (D-01)</name>
  <files>tecponto_app/tecponto/frontend/test_frontend_api.py</files>
  <precondition>The local test container `tecponto-local-server` is running and serving site `local-ci.local` (start it with `./scripts/dev-local-server.sh up` if `docker ps` does not list it).</precondition>
  <read_first>
    - `tecponto_app/tecponto/frontend/test_frontend_api.py:316-387` — the existing wiring block inside `run_foundation_checks`; every line is a bare local assignment `name = run_x_checks()`.
    - `tecponto_app/tecponto/frontend/test_frontend_api.py:388-463` — the `return {...}` dict where every one of those locals is re-exported under a snake_case key.
    - `tecponto_app/tecponto/frontend/test_frontend_api.py:2369` (`run_warranty_mode_checks`), `:5650` (`run_pos_sale_checks`), `:5820` (`run_pos_barcode_label_checks`), `:5935` (`run_pos_retail_barcode_catalog_checks`) — read at least each `def` line plus its first 5 lines to confirm all four take no arguments and return a dict.
  </read_first>
  <action>
Per D-01 in 03-CONTEXT.md, add exactly four parameterless calls inside `run_foundation_checks`, following the identical shape of the neighbouring assignments at lines 343-354 (one local variable per check, no arguments):

- `pos_sale_check = run_pos_sale_checks()`
- `pos_barcode_label_check = run_pos_barcode_label_checks()`
- `pos_retail_barcode_catalog_check = run_pos_retail_barcode_catalog_checks()`
- `warranty_mode_check = run_warranty_mode_checks()`

Placement (readability only — these are independent assignments with no ordering dependency, per PATTERNS.md): put the three `pos_*` assignments adjacent to `pos_cost_guard = _check_pos_item_cost_guard(...)` at line 356, and `warranty_mode_check` adjacent to `used_device_warranty_lookup = run_used_device_warranty_lookup_checks()` at line 345.

Then add one key per new local to the `return {...}` dict that starts at line 388, using the same snake_case key-equals-variable convention every existing entry uses: `"pos_sale": pos_sale_check`, `"pos_barcode_label": pos_barcode_label_check`, `"pos_retail_barcode_catalog": pos_retail_barcode_catalog_check`, `"warranty_mode": warranty_mode_check`.

Do not modify the bodies of the four check functions. All four were confirmed passing standalone against the current codebase during phase discussion; if one now fails, that is a genuine regression to investigate and report, not a signal to skip the wiring.

This is the tracer slice for the phase: it drives one thin path (suite entry point -> check function -> real PDV/warranty engine -> real invoice/stock/GL/PDF) end to end through every layer this phase will later touch, so every subsequent plan lands on an already-watched surface.
  </action>
  <verify>
    <automated>wsl -d Ubuntu -e bash -c "cd /home/usuario/frappe-bench/apps/tecponto_app &amp;&amp; ./scripts/dev-local-server.sh restart &amp;&amp; docker exec tecponto-local-server bash -lc 'cd /home/frappe/frappe-bench &amp;&amp; bench --site local-ci.local execute tecponto_app.tecponto.frontend.test_frontend_api.run_foundation_checks' > /tmp/tp-foundation.txt &amp;&amp; grep -q pos_sale /tmp/tp-foundation.txt &amp;&amp; grep -q pos_barcode_label /tmp/tp-foundation.txt &amp;&amp; grep -q pos_retail_barcode_catalog /tmp/tp-foundation.txt &amp;&amp; grep -q warranty_mode /tmp/tp-foundation.txt &amp;&amp; echo WIRED-OK"</automated>
    <fails_when>non-zero exit; an AssertionError or traceback anywhere in `/tmp/tp-foundation.txt` (the check functions raise internally, which aborts the run before the greps); or `WIRED-OK` never printing because one of the four keys is absent from the returned dict.</fails_when>
  </verify>
  <acceptance_criteria>
    - `run_foundation_checks` contains one call each to `run_pos_sale_checks`, `run_pos_barcode_label_checks`, `run_pos_retail_barcode_catalog_checks` and `run_warranty_mode_checks`.
    - The dict returned by `run_foundation_checks` contains all four keys `pos_sale`, `pos_barcode_label`, `pos_retail_barcode_catalog`, `warranty_mode`, and `status` is `"ok"`.
    - The bodies of the four check functions are byte-identical to their pre-task state (`git diff` shows changes only inside `run_foundation_checks`).
  </acceptance_criteria>
  <done>`run_foundation_checks` returns `status: "ok"` and its dict exposes the four new keys; `git diff --stat` touches only `test_frontend_api.py`.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the concurrent cash-session block inside CI (D-02, AUDIT-02)</name>
  <files>tecponto_app/tecponto/frontend/test_frontend_api.py</files>
  <read_first>
    - `tecponto_app/tecponto/cash.py:31-64` — `open_cash_session`; note the TWO distinct early-return branches: the `opening_idempotency_key` replay branch (lines 45-54, which returns a summary and does NOT raise) and the `session_key` collision branch (lines 56-64, which raises `frappe.ValidationError` with `"Já existe um caixa aberto no ponto {0}."`). The new assertion must hit the second, so it MUST pass a fresh `idempotency_key` and the SAME `cash_point`.
    - `tecponto_app/tecponto/frontend/test_frontend_api.py:7415-7516` — `run_cash_session_checks`; `cash_point` is created at line 7423 and first opened at lines 7427-7432. Lines 7493-7500 are the existing role-gate assertion whose try/except/`raise AssertionError` shape this new assertion copies.
    - `tecponto_app/tecponto/frontend/test_frontend_api.py:7652` (`run_cash_closing_checks` docstring + assertions) — confirm the existing undocumented-divergence rejection assertion is present and returned; this task does not change it, it only records that AUDIT-02's divergence half is already covered.
  </read_first>
  <action>
Per D-02 in 03-CONTEXT.md: the concurrent-session block already exists in production code (`cash.py:56-64`, a hard `frappe.throw`, stricter than AUDIT-02's original "warns" wording and therefore acceptable). No production change is expected — this task closes the test-coverage gap only.

Extend the existing `run_cash_session_checks` (extend-don't-duplicate, the pattern Phase 1 established in its D-01). Insert, after the attendant is the active user and before the `return {...}` at line 7506 (a natural spot is immediately after the `frappe.set_user(attendant)` at line 7502):

- Set a boolean `second_open_blocked = False`.
- In a `try`, call `open_cash_session` with `opening_amount=50`, a FRESH `idempotency_key=f"tp-cash-concurrent-{frappe.generate_hash(length=20)}"`, `opened_by=attendant`, and `cash_point=cash_point` — reusing the exact `cash_point` local already opened earlier in this function so the `session_key` collides on the same company/cash_point/business_date.
- `except frappe.ValidationError:` set `second_open_blocked = True`.
- After the try/except, `raise AssertionError("Segunda abertura de caixa no mesmo ponto não foi bloqueada.")` when `second_open_blocked` is falsy — the same shape as the `technician_blocked` assertion at lines 7494-7500.
- Add `"second_open_blocked": second_open_blocked` to this function's own `return {...}` dict.

Guard against a false pass: if the call returns normally with `idempotent_replay` true, the test hit the idempotency branch instead of the session_key branch — that must still fail the assertion, so do NOT catch a successful return as evidence of blocking.

If `run_cash_closing_checks` turns out NOT to expose its divergence-rejection result in its return dict, add the corresponding boolean key to that dict too (no behavioural change, just make the existing proof legible in the suite output). Do not weaken or reroute any existing assertion.
  </action>
  <verify>
    <automated>wsl -d Ubuntu -e bash -c "cd /home/usuario/frappe-bench/apps/tecponto_app &amp;&amp; ./scripts/dev-local-server.sh restart &amp;&amp; docker exec tecponto-local-server bash -lc 'cd /home/frappe/frappe-bench &amp;&amp; bench --site local-ci.local execute tecponto_app.tecponto.frontend.test_frontend_api.run_cash_session_checks' > /tmp/tp-cash.txt &amp;&amp; grep -q second_open_blocked /tmp/tp-cash.txt &amp;&amp; echo CONCURRENT-BLOCK-PROVEN &amp;&amp; docker exec tecponto-local-server bash -lc 'cd /home/frappe/frappe-bench &amp;&amp; bench --site local-ci.local execute tecponto_app.tecponto.frontend.test_frontend_api.run_cash_closing_checks'"</automated>
    <fails_when>non-zero exit; any AssertionError or traceback in the output (the function raises internally when the second opening is NOT blocked, aborting before the grep, so a printed `CONCURRENT-BLOCK-PROVEN` means both that the key exists and that the assertion held); `CONCURRENT-BLOCK-PROVEN` never printing; or `run_cash_closing_checks` stopping returning its previous successful result.</fails_when>
  </verify>
  <acceptance_criteria>
    - `run_cash_session_checks` returns a dict containing `second_open_blocked: True`.
    - The second `open_cash_session` call in that function uses a different `idempotency_key` than the first one and the same `cash_point` local.
    - `run_cash_closing_checks` still completes successfully and its undocumented-divergence rejection assertion is unchanged.
    - No file under `tecponto_app/tecponto/cash.py` was modified.
  </acceptance_criteria>
  <done>CI proves both halves of AUDIT-02: closing with an undocumented divergence is rejected, and a second concurrent session at the same cash point is rejected.</done>
</task>

</tasks>

## Artifacts this phase produces

Phase-wide roster (index for all five plans; each later plan repeats only its own slice).

| Symbol | Kind | File | Plan |
|---|---|---|---|
| `pos_sale`, `pos_barcode_label`, `pos_retail_barcode_catalog`, `warranty_mode` | new keys in `run_foundation_checks` return dict | `tecponto_app/tecponto/frontend/test_frontend_api.py` | 03-01 |
| `second_open_blocked` | new key in `run_cash_session_checks` return dict | `tecponto_app/tecponto/frontend/test_frontend_api.py` | 03-01 |
| `run_pos_tradein_cost_guard_checks` | new test function | `tecponto_app/tecponto/frontend/test_frontend_api.py` | 03-02 |
| `pos_tradein_cost_guard` | new key in `run_foundation_checks` return dict | `tecponto_app/tecponto/frontend/test_frontend_api.py` | 03-02 |
| `CHECKLIST_RESULT_VALUES` | new module constant | `tecponto_app/tecponto/tradein/evaluation.py` | 03-03 |
| `_validate_checklist_complete` | new validation function | `tecponto_app/tecponto/tradein/evaluation.py` | 03-03 |
| `checklist_template` | new module function returning the expected rows for a device type | `tecponto_app/tecponto/tradein/evaluation.py` | 03-03 |
| `get_tradein_checklist_template` | new whitelisted endpoint | `tecponto_app/tecponto/frontend/api.py` | 03-03 |
| `set_tradein_checklist_results` | new whitelisted endpoint | `tecponto_app/tecponto/frontend/api.py` | 03-03 |
| `_get_evaluation_checklist` | new private serializer helper | `tecponto_app/tecponto/frontend/api.py` | 03-03 |
| `checklist` | new optional key on the `create_trade_evaluation` payload and on the serialized evaluation | `tecponto_app/tecponto/frontend/api.py` | 03-03 |
| `TradeEvaluationChecklistRow` | new TS interface | `frontend/src/api/types.ts` | 03-03 |
| `getTradeinChecklistTemplate`, `setTradeinChecklistResults` | new TS client methods | `frontend/src/api/balcao.ts` | 03-03 |
| `checklist_incomplete_blocked` | new key in `run_tradein_frontend_checks` return dict | `tecponto_app/tecponto/frontend/test_frontend_api.py` | 03-03 |
| `used_device_warranty` | new Service Order field (Link -> Used Device Warranty) | `tecponto_app/tecponto/doctype/service_order/service_order.json` | 03-04 |
| `used_device_warranty_no_charge` | new Service Order field (Check) — the courtesy/no-charge marker for AUDIT-05 | `tecponto_app/tecponto/doctype/service_order/service_order.json` | 03-04 |
| `is_warranty_active` | new shared helper (role-free expiry comparison, reused by `consultar_garantia_usado`) | `tecponto_app/tecponto/used_device_warranty.py` | 03-04 |
| `_validate_used_device_warranty_no_charge` | new validation function | `tecponto_app/tecponto/service_order/policies.py` | 03-04 |
| `_apply_used_device_warranty_coverage` | new private check-in helper | `tecponto_app/tecponto/frontend/api.py` | 03-04 |
| `used_device_warranty`, `used_device_warranty_no_charge` | new keys on the Service Order detail payload | `tecponto_app/tecponto/frontend/api.py` | 03-04 |
| `run_used_device_warranty_claim_checks` | new test function | `tecponto_app/tecponto/frontend/test_frontend_api.py` | 03-04 |
| `used_device_warranty_claim` | new key in `run_foundation_checks` return dict | `tecponto_app/tecponto/frontend/test_frontend_api.py` | 03-04 |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| React counter -> `frontend/api.py` / `frontend/pos.py` whitelisted endpoints | Untrusted role-bearing input crosses here; every gate must live on the Python side (GEMINI.md section 1.3) |
| CI suite -> production engine | The suite is the only automated proof that the gates above still fire |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Repudiation | `run_foundation_checks` (dead PDV/warranty coverage) | high | mitigate | Task 1 wires the 4 orphaned checks so PDV stock/GL/idempotency/cost-guard regressions can no longer land unnoticed |
| T-03-02 | Tampering | `cash.py::open_cash_session` concurrent opening | high | mitigate | Task 2 proves the existing `session_key` hard block fires, so a second drawer cannot be opened at the same point/date and absorb cash outside the audited session |
| T-03-03 | Information disclosure | PDV payloads reaching Atendente/Gestor/Técnico | high | mitigate | The wired `run_pos_sale_checks` / `run_warranty_mode_checks` already carry `contains_sensitive_field` guards; wiring makes those guards continuously enforced (extended systematically in 03-02) |
| T-03-SC | Tampering | npm/pip/cargo installs | low | accept | This phase installs no packages — every change is in already-tracked first-party source. If any task later needs a dependency, stop and run the package-legitimacy gate first |
</threat_model>

<verification>
- `run_foundation_checks` completes with `status: "ok"` and exposes the four new PDV/warranty keys.
- `run_cash_session_checks` returns `second_open_blocked: True`.
- `run_cash_closing_checks` still passes unchanged.
- `git diff --stat` for this plan touches only `tecponto_app/tecponto/frontend/test_frontend_api.py`.
</verification>

<success_criteria>
- AUDIT-01's PDV coverage (sale, barcode label, retail barcode catalog) and the repair-warranty-mode coverage run on every suite execution instead of never.
- AUDIT-02 is fully proven in CI: undocumented-divergence closing rejected AND concurrent session opening rejected.
- No production Python file was changed by this plan.
</success_criteria>

<output>
Create `.planning/phases/03-systematic-audit-pdv-trade-in-warranty-aparelho-usado-caixa/03-01-SUMMARY.md` when done.
Commit atomically per GEMINI.md section 2.3, e.g. `test(03-01): wire orphaned PDV/warranty checks and prove concurrent cash block`.
</output>
